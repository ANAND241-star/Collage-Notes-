# ============================================================
#  NoteVault — Environment Variables
#  LOCAL: rename this file to .env
#  VERCEL: See VERCEL_SETUP.md for instructions
# ============================================================

# JWT Secret Key — generate a random string
# Run: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-min-32-chars

# MongoDB Atlas Connection String
# Format: mongodb+srv://user:password@cluster.mongodb.net/database
# See VERCEL_SETUP.md for detailed instructions
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/notvault?retryWrites=true&w=majority

# JWT Token lifetime (hours)
JWT_EXPIRY_HOURS=24

# CORS — for Vercel: https://collage-notesb.vercel.app,http://localhost:3000
# For local dev: *
ALLOWED_ORIGINS=*

# Local dev only (Vercel pe daalne ki zaroorat nahi)
FLASK_DEBUG=false
PORT=5000

