# ============================================================
#  NoteVault — Environment Variables
#  LOCAL: rename this file to .env
#  VERCEL: in jaake Project Settings > Environment Variables mein daalo
# ============================================================

# JWT Secret Key — koi bhi long random string likho
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=notvault-change-this-to-a-long-random-secret-key-123

# MongoDB Atlas Connection String
# Steps:
#   1. cloud.mongodb.com pe free account banao
#   2. M0 Free cluster banao
#   3. Database Access > Add User (username + password)
#   4. Network Access > Allow 0.0.0.0/0
#   5. Connect > Drivers > Python > copy URI
#   6. Replace <password> with your password, add /notvault at end
MONGO_URI=mongodb+srv://anandjatt689_db_user:iYxGoSsl8xjOmdv1@cluster1.pn1gnfx.mongodb.net/?appName=Cluster1

# JWT Token lifetime (hours)
JWT_EXPIRY_HOURS=24

# CORS — development mein * rakho, production mein frontend URL daalo
ALLOWED_ORIGINS=*

# Local dev only (Vercel pe daalne ki zaroorat nahi)
FLASK_DEBUG=false
PORT=5000
